package dev.nkudrin.slogar

/** Defines the rule set used to determine syllable boundaries. */
enum class SyllablePolicy {
    /** Uses deterministic school-style written-word syllabification rules. */
    SchoolSimple,
}
